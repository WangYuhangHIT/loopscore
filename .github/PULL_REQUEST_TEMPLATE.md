## What & why

<!-- What does this change and why? Link any related issue. -->

## Checklist

- [ ] `node --test` passes
- [ ] Frontend builds (`npm --prefix frontend run build`) if the UI changed
- [ ] No runtime dependency added to the root `package.json`
- [ ] No secrets / API keys in the diff
- [ ] New logic has a test
